- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.gitconfig: gitconfig
    ~/.sbt: sbt/
    ~/.aws: aws/
    ~/.ansible: ansible/

- shell:
  - [git submodule update --init --recursive, Installing submodules]
