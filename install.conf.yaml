- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.gitconfig: gitconfig
    ~/.sbt: sbt/
    ~/.aws: aws/
    ~/.ansible: ansible/
    ~/.oh-my-fish: oh-my-fish/

- shell:
  - [git submodule update --init --recursive, Installing submodules]
